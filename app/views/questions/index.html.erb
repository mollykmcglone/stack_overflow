<h1> (The real) STACK OVERFLOW</h1>

  <ul>
    <% @questions.each do |question| %>
      <li>
        <h3><%= link_to question.title, question_path(question) %></h3>
        <p><%= question.content %></p>
      </li>
    <% end %>
  </ul>

<p> <%= link_to "Ask a new question", new_question_path  %></p>
